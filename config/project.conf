# Example config (key=value). Lines starting with # or ; are comments.
#
# Recommended precedence:
#   CLI args > this config file > built-in defaults

# Phase 1 (screening)
phase1_input_dir=hidden-ins-raw
phase1_output_dir=experiments/phase_1/bitmap_results
phase1_max_files=256
phase1_timeout_seconds=7200

# Phase 2 (sandbox analysis / functional tests)
# Directory containing the screened results from Phase 1
phase2_input_dir=experiments/phase_1/decoded_results
# Base output dir for phase2 results.
phase2_output_dir=experiments/phase_2
